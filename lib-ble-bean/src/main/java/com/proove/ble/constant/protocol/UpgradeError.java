package com.proove.ble.constant.protocol;

public enum UpgradeError {
    UPGRADE_INFO_ERROR,
    NETWORK_ERROR,
    DOWNLOAD_FILE_ERROR,
    UPGRADE_ERROR,
    LOW_BATTERY,
    OLD_VERSION_UPGRADE_ERROR,
    BLUETOOTH_DISABLE
}
